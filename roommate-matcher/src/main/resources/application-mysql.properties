# MySQL Database Configuration (Remote Server)
# To use MySQL, run with: --spring.profiles.active=mysql

# MySQL Connection - Friend's Remote Database
spring.datasource.url=jdbc:mysql://172.31.57.159:3306/roommate_matchr_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=roommate_user
spring.datasource.password=roommate_pass
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Connection Pool Settings (important for remote DB)
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=10

# Disable H2 console in MySQL mode
spring.h2.console.enabled=false

# JWT Configuration (must be included)
jwt.secret=RoomMatcherUMassCS520Group8SecretKeyForJWTAuthentication2024
jwt.expiration=86400000
